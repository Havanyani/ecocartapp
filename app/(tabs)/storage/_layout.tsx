import { Slot } from 'expo-router';
import React from 'react';

/**
 * Storage Layout
 * 
 * Defines the navigation structure for the Storage tab
 * Using Slot to render child routes
 */
export default function StorageLayout() {
  return <Slot />;
} 