/**
 * Index file for the Router component.
 * Exports the platform-specific Router component and its types.
 */

export { Router } from './Router';
export type { Route, RouterProps } from './Router';
